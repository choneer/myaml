# Changelog

本文件记录 `choneer/myaml` 相对上游模板的个人化调整。

## 2026-08-12

### 初始化仓库

- 建立个人自用 OpenClash 配置仓库。
- 上游来源：`Aethersailor/Custom_OpenClash_Rules`。
- 原始模板参考：`cfg/Custom_Clash_Full.ini`。

### 策略组调整

- 保留原 `手动选择`。
- 新增 `手动选择2`。
- 新增 `手动选择3`。
- 三个手动选择组均保留 `include-all: true`，可直接选择 OpenClash 注入的实际节点。

### 链式代理相关

- `机场前置` 重命名为 `链式前置`。
- `链式前置` 增加 `DIRECT` 选项。
- `美国落地` 重命名为 `链式落地`。
- `链式落地` 改为仅匹配名称包含 `小鸡` 的节点。
- 普通自动选择及地区节点组排除名称包含 `小鸡` 的节点，避免落地节点混入链式前置候选。
- `手动选择`、`手动选择2`、`手动选择3` 不排除 `小鸡`，因此仍可直接选择具体落地节点。
- 完整链式关系由 OpenClash 外部链式配置负责，约定为：`链式前置 → 链式落地`。

### 个人使用习惯

- `漏网之鱼` 默认优先选择 `手动选择`，使未匹配规则的非 CN 流量默认进入代理选择。
- `非标端口` 默认优先选择 `全球直连`。
- 上述两项均为个人长期实际使用习惯，属于有意偏离上游默认行为。

### 校验

- YAML 可正常解析。
- 策略组引用检查通过。
- 规则目标引用检查通过，无缺失策略组引用。
